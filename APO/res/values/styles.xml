<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:textColor">#323232</item>
        <item name="android:typeface">normal</item>
        <item name="android:windowBackground">@color/appBackgroundColor</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
    </style>
    
    <style name="MyActionBar" parent="android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/actionBarColor</item>
        <item name="android:titleTextStyle">@style/myTitleTextStyle</item>
        <item name="android:icon">@drawable/main_icon</item>
    </style>
    
    <style name="myTitleTextStyle" parent="android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/lightGrayColor</item>
        <item name="android:textStyle">bold</item>
    </style>
 
    <style name="myNavDrawer" parent="android:style/Widget.Holo.Light.ListView">
        <item name="android:background">@color/navDrawerColor</item>
    </style>

</resources>
